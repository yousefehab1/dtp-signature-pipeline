% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{apply_fdr}
\alias{apply_fdr}
\title{Apply Benjamini-Hochberg FDR correction within a named grouping}
\usage{
apply_fdr(stats_df, cfg, family = c("cohort", "pancan"), p_col = "Raw_P")
}
\arguments{
\item{stats_df}{A stats table from \code{\link[=run_all_stats]{run_all_stats()}}/\code{\link[=run_survival_block]{run_survival_block()}} or equivalent.}

\item{cfg}{Config from \code{\link[=dtp_config]{dtp_config()}}.}

\item{family}{"cohort" or "pancan" -- selects \code{cfg$fdr_by_cohort}/\code{cfg$fdr_by_pancan}.}

\item{p_col}{P-value column BH acts on. Most families use "Raw_P"; the adjusted-Cox (confounding)
family should pass \code{p_col = "LRT_score_P"} so its FDR is on the likelihood-ratio test of score vs.
the covariate-only model, matching the interaction family's use of an LRT rather than a Wald p.}
}
\description{
The grouping is a closed enum, not a free-form \verb{by=} vector: \code{family = "cohort"} groups by
\code{cfg$fdr_by_cohort} (used by the single-cohort CRC arm), \code{family = "pancan"} groups by
\code{cfg$fdr_by_pancan} (used by the cross-cohort pan-cancer arm). The two groupings are deliberately
different -- they answer different statistical questions -- but making the choice an enum backed by
cfg instead of an ad-hoc argument at each call site means no call site can silently pass a third,
inconsistent grouping.
}
