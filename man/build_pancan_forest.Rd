% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pancan_composites.R
\name{build_pancan_forest}
\alias{build_pancan_forest}
\title{Build per-cohort pan-cancer survival forest plot for a single score}
\usage{
build_pancan_forest(
  stats_df,
  master,
  score = "Up_ssGSEA",
  score_lab = "DTP Up",
  order_score = score
)
}
\arguments{
\item{stats_df}{FDR stats summary tibble from \code{\link[=run_pancan_survival]{run_pancan_survival()}}.}

\item{master}{Scored master clinical tibble from \code{\link[=run_pancan_survival]{run_pancan_survival()}}.}

\item{score}{Name of the score column to plot (e.g. "Up_ssGSEA").}

\item{score_lab}{Human-readable score label (e.g. "DTP Up").}

\item{order_score}{Name of the score column used to sort cohort rows (defaults to \code{score}).}
}
\value{
A ggplot object.
}
\description{
Generates a univariable continuous-score Cox proportional hazards forest plot
across all testable TCGA cohorts (per 1 SD), faceted by survival endpoint
(OS and RFS), highlighting TCGA-COAD.
}
