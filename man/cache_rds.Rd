% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{cache_rds}
\alias{cache_rds}
\title{Disk-backed cache with legacy warm-through}
\usage{
cache_rds(cfg, key, fun, vary_on = NULL)
}
\arguments{
\item{cfg}{Config from \code{\link[=dtp_config]{dtp_config()}}.}

\item{key}{Cache key; sanitized to a safe filename.}

\item{fun}{Zero-argument function producing the value to cache.}

\item{vary_on}{Optional list of config-derived values the cached value
depends on (e.g. \code{list(id_type = cfg$id_type)}). Included in the cache
filename via a short hash. Omit for raw downloads that are independent
of \code{cfg} and should warm-hit the legacy cache directly.}
}
\description{
\code{fun} only runs on a cache miss. On a miss, first checks
\code{cfg$legacy_cache_dir} (the old thesis project's cache) for the same key
and copies it in if found there, so raw GEO/GDC downloads are never
re-fetched. Entries whose content depends on config (e.g. \code{id_type} or the
signature panel) must pass \code{vary_on} -- this hashes those values into the
filename so a config change can never silently load a stale, incompatible
cache entry (the legacy project's \code{cache_rds()} had no such guard).
}
