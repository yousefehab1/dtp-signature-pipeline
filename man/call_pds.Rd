% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subtyping.R
\name{call_pds}
\alias{call_pds}
\title{Classify CRC samples into Pathway-Derived Subtypes (PDS)}
\usage{
call_pds(emat_symbol_log2, species = "human", threshold = 0.6)
}
\arguments{
\item{emat_symbol_log2}{A log2-scale expression matrix with gene symbols as row names.}

\item{species}{Species name passed to \code{PDSclassifier::PDSpredict} (default \code{"human"}).}

\item{threshold}{Posterior probability threshold for assigning PDS1/2/3 (default 0.6).}
}
\value{
A data frame with columns \code{Sample_ID} and \code{PDS}, or \code{NULL} if PDSclassifier is unavailable.
}
\description{
Runs PDSclassifier if installed; degrades gracefully returning \code{NULL} if absent.
}
